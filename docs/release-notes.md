Главный экран переехал в новый двухколоночный макет: слева — drop-зона с чипами форматов (MP3/M4A/MP4/MOV/WAV), YouTube-вход с настоящим брендовым лого и подсказки про приватность и горячие клавиши; справа — история транскриптов с медиа-иконками по расширению, абсолютными датами и кнопкой «Очистить» (новый IPC `clear_history`), статус-карточка движка и tip-card. Шрифт переключён на нативный macOS-стек (-apple-system / SF Pro) с тонкими весами и аккуратной типографикой. Dictation-pill пересобран: микрофон, kbd-блоки шортката, статус-текст и LED-индикатор справа. Под капотом — guard на `__TAURI_INTERNALS__` в `useAutoUpdate` и `useHistory`, чтобы фронт можно было гонять в обычном vite preview.
